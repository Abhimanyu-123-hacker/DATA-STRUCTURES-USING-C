#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

      int N;
    scanf("%d",&N);
    int arr[N];
    for(int i=0;i<N;i++){
        scanf("%d",&arr[i]);
        
    }
    int key,flag=0;
    scanf("%d ",&key);
    
    for(int i=0;i<N;i++){
        if(arr[i]==key){
            flag=1;
            break;
        }
    }
    if(flag==1)
        printf("Found");
    else 
        printf("Not Found");
        
        
  
       
    return 0; 
}
